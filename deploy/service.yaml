apiVersion: v1
kind: Service
metadata:
  name: servus-region-frontend
spec:
  type: ClusterIP
  selector:
    app: servus-region-frontend
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
